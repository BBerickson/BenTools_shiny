
load file and build data list ... plot with controls


legend spaceing
save plot ... make markdown file?

load gene lists, info update and plot controls working ... use state[2] ... loads data file needs to change the two select lists back to common
not common hide norm factor

load color files (add option for names to match?)

gene tools ...

# add more lines and lables presets
# test loading multiple files
# load file has spiner while running
# file size restriction the set correctly?
# load remote files TODO
# warning messages TODO
# try and have nickname not trigger apply math only update plot
# load bg files TODO
# restrict gene list to all loaded files or just plotted option? 
# if y slider gets too cumbersum the switch to numiricInputs
numericInput("inNumber", "Number input:",
                   min = 1, max = 20, value = 5, step = 0.5),
      numericInput("inNumber2", "Number input 2:",
                   min = 1, max = 20, value = 5, step = 0.5),